version: '3'

services:
  view-counter:
    build:
      dockerfile: Dockerfile
    environment:
      - DB_DSN=assets/storage.db
      - PORT=13004
    volumes:
      - "./assets/:./assets/"
    ports:
      - "13004:13004"